<Window x:Class="FUMiniTikiSystem.ProductManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Management" Height="550" Width="550"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#f8f8f8" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="16,16,16,8" Grid.Row="0">
            <TextBlock Text="Category:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
            <ComboBox Name="CategoryComboBox" Width="180" SelectionChanged="CategoryComboBox_SelectionChanged"/>
        </StackPanel>
        <TextBox Name="SearchBox"
                 Margin="16,0,16,8"
                 Height="36"
                 FontSize="16"
                 Padding="8"
                 VerticalContentAlignment="Center"
                 TextChanged="SearchBox_TextChanged"
                 Grid.Row="1"/>
        <Border Grid.Row="2" Margin="16" Background="White" CornerRadius="8" Padding="8">
            <ListView Name="ProductList" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Image Source="{Binding ImageUrl}" Width="60" Height="60" Margin="0,0,8,0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Price, StringFormat='Price: {0:C}'}"/>
                                <TextBlock Text="{Binding Description}" FontStyle="Italic"/>
                                <TextBlock Text="{Binding CategoryName}" Foreground="Gray"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>
